<resources>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>

        <item name="colorAccent">@color/accent</item>
        <item name="colorControlNormal">@color/primary_text</item>
        <item name="colorControlHighlight">@color/secondary_text</item>

        <item name="colorControlActivated">@color/primary_dark</item>
        <item name="colorSwitchThumbNormal">@color/accent</item>
        <item name="android:colorForeground">@color/primary</item>

    </style>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.TooBar" parent="AppTheme" />

    <style name="AppTheme.Scrollbar" parent="AppTheme" >
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">insideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/shape_scrollbar</item>
    </style>

    <style name="AppTheme.PopupMenu" parent="Widget.AppCompat.PopupMenu" >
        <item name="android:colorBackground">@color/primary_light</item>
        <item name="android:textColor">@color/primary_dark</item>

    </style>
</resources>
